# Email Configuration
# These environment variables are required for the email service to work

# Production (Resend)
RESEND_API_KEY=re_your_resend_api_key_here

# Development (Nodemailer SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Common
FROM_EMAIL=noreply@activpass.in

# Example configurations for different providers:

# Gmail SMTP (recommended for development)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password (use App Password, not regular password)

# Outlook/Hotmail SMTP
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Yahoo SMTP
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_USER=your-email@yahoo.com
# SMTP_PASS=your-app-password

# Ethereal Email (for testing - creates fake SMTP)
# No configuration needed, just remove SMTP_USER and SMTP_PASS
# The service will automatically use Ethereal for testing

# MailHog (local testing SMTP server)
# SMTP_HOST=localhost
# SMTP_PORT=1025
# No SMTP_USER and SMTP_PASS needed
