import { Loading } from '@paalan/react-ui';

const LoadingComponent = () => {
  return <Loading className="size-5" parentClassName="flex justify-center" content="Loading..." />;
};

export default LoadingComponent;
